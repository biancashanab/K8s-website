apiVersion: v1
kind: PersistentVolume
metadata:
  name: cms-db-pv
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/cms-db-data" # Use a different directory
